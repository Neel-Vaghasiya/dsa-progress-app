const User = require('./User');
const Topic = require('./Topic');
const Problem = require('./Problem');
const UserProgress = require('./UserProgress');

module.exports = { User, Topic, Problem, UserProgress };
